# --- Environment & Configuration ---
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings

# --- Core Web Framework ---
fastapi
uvicorn

# --- LangChain Ecosystem (Core) ---
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-text-splitters>=0.3.0
langchain-openai>=0.2.0
langchain-chroma>=0.1.0

# --- LangGraph & Agents (Explicitly included) ---
langgraph>=0.2.0
langgraph-checkpoint
langgraph-prebuilt
langgraph-sdk
langsmith

# --- Vector Database ---
chromadb>=0.5.0

# --- Data Processing ---
pandas
numpy>=1.24.0

# --- Web Requests & Networking (Restored) ---
requests
requests-oauthlib
httpx
httpx-sse             # Server-Sent Events (Streaming용)
aiohttp               # Async HTTP requests

# --- Security & Crypto (Restored) ---
cryptography          # SSL 인증서 관련

# --- AI Utilities (Recommended) ---
openai                # Direct OpenAI Client (LangChain 외 별도 사용 시)
tiktoken              # Token counting (OpenAI 모델 사용 시 필수 권장)
